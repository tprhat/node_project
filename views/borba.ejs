<!DOCTYPE html>
<html>

<head>
    <title><%= title%></title>
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

</head>

<header>

    <div class=" d-flex  container-fluid justify-content-between" style="background-color: rgb(42, 165, 73);"
        role="group">
        <a href="/igrac"><button type="button" class="btn btn-success btn-lg">Meni</button></a>
        <a href="/igrac/map"><button type="button" class="btn btn-success btn-lg">Karta</button></a>
        <a href="/igrac/upravljanjeKartama"><button type="button" class="btn btn-success btn-lg">Kolekcija</button></a>
        <a href="/pregledObavijesti"><button type="button" class="btn btn-success btn-lg">Obavjesti</button></a>
        <a href="/logout"><button type="button " class="btn btn-success btn-lg">Logout</button> </a>
    </div>
</header>

<body>
    <div class="container-fluid d-flex flex-column">
        <div class="">
            <h1>
                Your open challenges:
            </h1>
        </div>
        <div class="d-flex flex-column ">

            <div class="" style="overflow-y: auto; max-height: 84vh;">
                <% if(dvoboji) {%>
                <form action="/igrac/borba" method="post">
                    <fieldset>
                        <label for="odabir protivnika">Answer challenge: </label>
                        <select name="protivnik" id="protivnik">
                        <% for (igrac of igraci) {%>
                            <% for (dvoboj of dvoboji) {%>
                            <% if (dvoboj.idIzazivac == igrac.idKorisnik) {%>
                                <option value="<%= igrac.username%>"> <%= igrac.username%></option>
                            <% } %>
                            <% } %>
                            <% } %>
                        </select>
                        <br><br>
                        <label for="karta1">Pick your 1. card: </label>
                        <select name="karta1" id="karta1">
                            <% for( karta of karte) { %>
                            <option value="<%= karta.imeLokacije%>"> <%= karta.imeLokacije%> </option>
                            <% } %>

                        </select>
                        <br>
                        <br>
                        <label for="karta2">Pick your 2. card: </label>
                        <select name="karta2" id="karta2">
                            <% for( karta of karte) { %>
                            <option value="<%= karta.imeLokacije%>"> <%= karta.imeLokacije%> </option>
                            <% } %>

                        </select>
                        <br>
                        <br>
                        <label for="karta3">Pick your 3. card: </label>
                        <select name="karta3" id="karta3">
                            <% for( karta of karte) { %>
                            <option value="<%= karta.imeLokacije%>"> <%= karta.imeLokacije%> </option>
                            <% } %>

                        </select>
                        <br><br>
                        <button type="submit" class="button">Battle</button>
                    </fieldset>
                </form>
                <% } else {%>
                    <p>You don't have any open challenges. <br> 
                    You can challenge someone by clicking <a href="/igrac/borba/zahtjev"> here</a>.</p>
                    <% } %>
            </div>
            <div style="">
                <b><a href="/igrac/borba/zahtjev">Challenge someone</a></b>
            </div>
        </div>
    </div>

</body>

</html>